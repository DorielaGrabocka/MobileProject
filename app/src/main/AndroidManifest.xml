<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.activities">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PocketUni">
        <activity android:name=".SettingsActvity"></activity>
        <activity android:name=".SearchStudentsActivity" />
        <activity android:name=".SearchCoursesActivity" />
        <activity android:name=".BookmarkedCoursesActivity" />
        <activity android:name=".MyCoursesActivity" />
        <activity android:name=".GradeAnalyticsActivity" />
        <activity android:name=".CoursePage" />
        <activity android:name=".MainActivity" />
        <activity android:name=".LogIn">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>